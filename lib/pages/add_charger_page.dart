import 'package:flutter/material.dart';

class AddCharger extends StatelessWidget {
  const AddCharger({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Charger'),
      ),
    );
  }
}
